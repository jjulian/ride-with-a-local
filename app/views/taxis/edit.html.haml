- content_for :head do
  = javascript_include_tag 'gears_init'

- title "Update Location"

- form_for Location.new do |f|
  = f.error_messages
  = hidden_field_tag :taxi_id, @taxi.id
  = hidden_field_tag :lat
  = hidden_field_tag :lon
  %p.message

